// The logo files live in the offline flavor's asset set, so the online build
// has none to point at and resolves every token to its remote logoURI.

export const tokenLogosIndex: Record<string, string> = {};
