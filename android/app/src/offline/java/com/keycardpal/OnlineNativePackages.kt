package com.keycardpal

import com.facebook.react.ReactPackage

/**
 * The offline flavor links none of the online-only native modules; see the full flavor's twin
 * and react-native.config.js.
 */
fun onlineNativePackages(): List<ReactPackage> = emptyList()
