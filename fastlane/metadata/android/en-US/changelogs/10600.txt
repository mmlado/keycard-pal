Add opt-in PIN pad scramble toggle in Settings (default: fixed layout) Add opt-in ENS reverse resolution: toggle + custom RPC URL in Settings, ENS name display on address rows, "no ens" indicator for unregistered addresses Add iOS camera native module (`CameraView`) for QR code scanning Add `NFCError` overlay for iOS NFC error and cancellation states Rename "Support development" section to "Buy me a coffee" with updated copy Fix system UI dark theme: navigation bar, status bar, and safe area background now match app background on all screens
