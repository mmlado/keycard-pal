Bump GitHub Actions: `checkout@v6`, `setup-node@v6`, `setup-java@v5`; use Node 22 in CI and release workflows Fix `createBundle*JsAndAssets` Metro env script not being generated when React Native Gradle plugin registers bundle tasks lazily; switch from `tasks.findByName` to `tasks.matching { }.configureEach`
